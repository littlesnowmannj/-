# dino-jump
用c++编译的一个项目，类似`chrome://dino`,c++初学者感兴趣的可以试一试
# 逻辑
用一个`vector`数组，随机1-8，如果是5就输出`x`,按下空格键更新`(bool)dino_type`，如果=1，跳起。
** 系统接口`system`不一，所以我分了两个版本。**
#v1.1 &以后版本
该程序离不开example.txt，并且会自动生成example.txt。example.txt用于存储最高分。
